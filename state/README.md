# Local experiment state

This directory is populated by `scripts/inspect_project.py`,
`scripts/create_experiment.py`, `scripts/run_experiment.py`, and
`scripts/record_result.py`.

Do not copy generated state out of the private office environment. It can
contain local paths, code hashes, station-level summaries, and experiment
lineage. The portable package intentionally ships with this README only.
